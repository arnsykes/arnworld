export default function Footer() {
  return (
    <footer className="py-10 text-center text-xs text-base-sub">
      © {new Date().getFullYear()} ARN — Built with React, Vite, Tailwind, Three.js
    </footer>
  )
}